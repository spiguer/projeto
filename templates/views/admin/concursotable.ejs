<% layout('../layouts/boileradmin')%>

<div class="container d-flex justify-content-center align-items-center mt-5">
    <div class="row">
        <div class="">
            <div class="card shadow">
                <div class="card-body">
                    <h3 class="card-title">Tabela de Cursos</h3>
                    <br><br>

                    <table class="table table-bordered">
                        <thead>
                            <tr>
                                <th>Id</th>
                                <th>Nome</th>
                                <th>Ações</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% concurso.forEach(concurso => { %>
                                <tr>
                                    <th><%= concurso.id %></th>
                                    <th><%= concurso.name %></th>
                                    <th>
                                    <form method="POST" action="/concursos/delete">
                                        <input type="hidden" name="id" value="<%= concurso._id %>">
                                        <button class="btn btn-danger">Apagar</button>
                                    </form>
                                    </th>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>